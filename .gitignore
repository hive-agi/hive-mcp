# Clojure
.cpcache/
.nrepl-port
*.jar
!lib/persistent-sorted-set-0.3.118.jar
target/

# IDE
.idea/
*.iml
.vscode/
# clj-kondo: track config.edn, hooks/ and imports/ (clojure-lsp resolves
# library macros through the imported configs); ignore regenerable state only.
.clj-kondo/.cache/
**/.clj-kondo/.cache/
.clj-kondo/.lock
.clj-kondo/inline-configs/
.lsp/

# OS
.DS_Store
*~

# Logs
*.log

# ...but *.log above also matches DIRECTORIES, and clj-kondo imports vendor
# configs into dirs named after the artifact (io.pedestal/pedestal.log/).
!.clj-kondo/imports/**

# JVM heap dumps and crash artifacts
*.hprof
java_pid*.hprof
hs_err_pid*
*.jfr

# Compiled Elisp
*.elc

# Test output
test-output/

# Local config
.clojure-mcp/
.dir-locals.el

# Generated diagrams
overarch/
melpa/

# Vector index backups
backups/

arxiv/

# MELPA clone for local testing
melpa/

pdfs/

# Codanna local index
.codanna/
.fastembed_cache/

# deprecated kanban
kanban.org

# Drone test artifacts
test/drone_test_file_*.clj
test/drone-test-file-*.txt

# Datalevin LMDB data (persistent KG storage)
data/

# python stuff
classes

**/__pycache__/*
__pycache__/
*/__pycache__

plans/

# Plan EDN artifacts (generated by lings, not source code)
src/hive_mcp/plan/*.edn

# Addon classpath manifests (local-only, not shipped to remote)
resources/META-INF/hive-addons/

# Sibling projects (formerly submodules, now independent repos)
claude-code-ide.el/
hive-mcp.wiki/
hive-hot
local.deps.edn
.worktrees/

# Retrieval experiment inputs are private by default. Only deliberately
# sanitized example fixtures may be tracked.
dev/experiments/**/fixtures/*.edn
!dev/experiments/**/fixtures/*.example.edn
dev/experiments/private/
dev/experiments/**/*.private.edn
dev/experiments/**/*.local.edn
.lsp/.cache/
kondo-sweep.tsv

# clj-kondo's --copy-configs drops a SECOND, redundant copy of every imported
# dependency config directly under .clj-kondo/<group>/, duplicating the curated
# set tracked under .clj-kondo/imports/. Ignore the top-level duplicates (they
# are regenerable and were never committed); the tracked dirs are re-included.
/.clj-kondo/*/
!/.clj-kondo/imports/
!/.clj-kondo/hooks/
!/.clj-kondo/hive_di/
!/.clj-kondo/hive_dsl/

# Browser / e2e run artifacts (screenshots, traces): generated, not source.
dev/artifacts/

# Claude Code transient worktree state (parallel to the ignored /.worktrees/).
.claude/worktrees/
local.deps.edn.bak-*
