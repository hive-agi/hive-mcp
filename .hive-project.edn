{:project-id  "hive-mcp"
 :aliases ["emacs-mcp"]
 :description "Hivemind MCP server for Claude Code + Emacs integration"

 ;; Source directories for hot-reload watching (recursive)
 :watch-dirs ["src" "hive-hot/src"]

 ;; Enable hot-reload for this Clojure project
 :hot-reload false

 ;; Knowledge Graph storage backend (:datascript | :datalevin)
 ;; Datalevin persists to data/kg/datalevin/ via LMDB
 :kg-backend :datalevin}
